function pops = Extract_Populations(units)

% function pops = Extract_Populations(units)
%
% returns list of arrays of units that have been recorded at the same time
%

nu=length(units);
current_mouse_counter=units(1).mouse_counter;
current_series_number=units(1).series_number;
for i=2:nu
  
end
